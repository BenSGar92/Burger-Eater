<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Devour It!{{else}}Order Again!{{/if}}
	</button>
	<div class="delete" data-id="{{id}}" data-newdelete="{{devour}}">
		{{#if devour}}{{else}}<button display="inline-block">Delete</button>{{/if}}
	</div>
</li>
